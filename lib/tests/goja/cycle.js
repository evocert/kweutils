define([
	"module",
	"console",
	"cyclejs"
],function(
	module,
	console,
	cycle
){
	console.log([module.id,'start'].join(':'));
	console.log(cycle);
	a={};
	a.a=a;
	console.log(cycle.decycle(a));
});
